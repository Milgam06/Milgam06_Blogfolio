import styled from "@emotion/styled";
import { motion } from "framer-motion";

export const TitleWrapperElement = styled(motion.div)`
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 1rem 2rem;
  background-color: #000;
  border-radius: 1rem;
  overflow: hidden;
`;
